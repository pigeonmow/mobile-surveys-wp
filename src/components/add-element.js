var React = require('react');

module.exports = React.createClass({
  displayName: 'AddElement',
  
  render: function () {
  
    return (
      <div>
        <h1>Add elements</h1>
      </div>
    );
  
  }
});